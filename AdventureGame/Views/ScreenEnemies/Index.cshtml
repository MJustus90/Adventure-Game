@model IEnumerable<AdventureGame.Models.ScreenEnemy>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScreenId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EnemyId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Action)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ScreenId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnemyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Action)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ScreenEnemyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ScreenEnemyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ScreenEnemyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
